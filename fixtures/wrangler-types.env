# Stable empty env file for `npm run types:worker`.
#
# Wrangler types reads local .dev.vars by default. The generated
# worker-configuration.d.ts should only contain Worker bindings from
# wrangler.jsonc; stable secret and override names live in env-augment.d.ts.
