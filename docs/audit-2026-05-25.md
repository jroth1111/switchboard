# 20-slice parallel bug and logic audit

**Date:** 2026-05-25  
**Scope:** Full switchboard codebase (~103 TS files), including working-tree WIP at audit start  
**Method:** 20 disjoint file slices, 2 waves of 10 parallel `generalPurpose` subagents, bug + logic rubric, fix-all policy, then repo gate + pass-2 integration

## Summary

| Metric | Result |
|--------|--------|
| Slices audited | 20 / 20 |
| Tests | **781 / 781** pass (`npm test`) |
| Typecheck | **clean** (`npm run tsc`) |
| Pass-2 integration fixes | 3 files (see below) |
| Final commit on `main` | `8ee3a73` |

## Pass-2 integration (parent agent)

| Fix | File | Reason |
|-----|------|--------|
| Top-level `fetch` try/catch + `withCors` on 500 | `src/index.ts` | Slice 01 blocked: thrown errors lacked CORS headers |
| `typeMatches` / `resolveSchemaForValue` for `type: string[]` | `src/nim/repair/schema-aware.ts` | Slice 20 introduced union types; `tsc` failed |
| OAuth lock test TTL 50ms / sleep 60ms | `tests/state/oauth-account-do.test.ts` | Flaky timing vs 20ms/30ms window |

## Per-slice findings and fixes

### Wave 1 (slices 01–10)

| Slice | High-severity fixes | Notes / blocked |
|-------|---------------------|-----------------|
| **01** Bootstrap | `ctx.waitUntil` for canary persistence; warn on DO prefetch failures; portable `failures.ts` main guard | CORS on fetch errors fixed in pass-2 |
| **02** HTTP handler | Admin health double-auth removed; `estimatePromptTokens` for string `input`; admin query param parsing (`since`/`until`/`limit`) | Hidden-only content on `/v1/responses` needs content-parts slice |
| **03** HTTP support | Non-object `messages[i]` → 400 instead of 500 | — |
| **04** Config manifest | `ROUTING_ONLY_ROUTE_GROUPS`; planner/orphan policy validation; script uses shared `validateManifest` | `npm run validate` needs secrets in env |
| **05** Runtime config | Flat OAuth JSON → `CHATGPT_AUTH_JSON`; path-valued `CHATGPT_AUTH_FILE` resolution | — |
| **06** Security | Top-level internal key redaction; array `metadata` handling; `Object.hasOwn` for hoist; decrypt payload guards | — |
| **07** Attempts | Attempt timeout capped by remaining deadline (no 5s floor past budget) | — |
| **08** Planner | Skip empty deployment groups (`nim-secondary`); half-open inflight cap in `deployment-filter` | Pre-filter not wired to request path yet |
| **09** State CP | Confirmed reservation TTL extended for streams; learned concurrency defaults after DO eviction | — |
| **10** Admission | `half_open` partial successes no longer decay `failureCount` | `halfOpenPenalty` on `AdmissionRequest` unused in `admit()` |

### Wave 2 (slices 11–20)

| Slice | High-severity fixes | Notes / blocked |
|-------|---------------------|-----------------|
| **11** OAuth/storage | `setToken` preserves refresh/expiry on partial updates; `Number(locked_until)` compare; in-memory learned-limit expiry parity | DO crypto tests need `ENCRYPTION_KEY` in vitest env |
| **12** Probes | `avgHealthScore` excludes blocked deployments; `circuitState` reflects `half_open`/`suspect` | Canary cooldown literals vs per-policy thresholds |
| **13** Streaming | Pre-buffer `committed` semantics; skip buffering non-delta pre-commit; UTF-8 decoder flush in parser/converter | — |
| **14** Observability | Embedded secrets in receipt strings; numeric token usage coercion; log field precedence | Denial receipts not in `/nim/failures` by design |
| **15** Anthropic | OAuth expiry consistency; double-check after refresh lock; validate `access_token` on refresh | `tokenUrl` not passed from handler |
| **16** ChatGPT | 400 token errors → `oauth_session_failure` (not context length via fallback) | — |
| **17** Providers core | Trailing-slash `apiBase`; reject non-object JSON bodies; streaming abort listener cleanup; OAuth test mock | — |
| **18** NIM classify/eval | Reject tool calls without `request.tools`; schema validation after repair; input-echo denominator guard | Broad `400`+`token` heuristic unchanged |
| **19** NIM repair (small) | Nested `<thinking>` strip; repetition empty-line sentinel | — |
| **20** NIM repair (heavy) | `deleteAtPath` array splice; optional-field for indexed paths; union type resolution; integer strictness; clone args before repair | Pass-2 tsc fix for `type: string[]` |

## Residual risks — resolved (2026-05-25 follow-up)

| Item | Resolution |
|------|------------|
| Planner `deployment-filter` | Wired into attempt loop + hedge admission via `buildFilterStateFromHealth`; suspect/quarantine parity added |
| Fixture worker | Shared `fixtureErrorResponse` for chat, responses, and messages paths |
| `classifyProviderFailure` | `isContextLengthError()` excludes OAuth/token auth bodies from context-length classification |
| `reapAllLeases` | Documented as single `control-plane` shard (by design) |
| `scripts/failures-probe.ts` | Removed (duplicate of `scripts/failures.ts`) |
| OAuth DO tests | `ENCRYPTION_KEY` in `wrangler.dev.jsonc` vars; crypto round-trip tests added |
| Canary cooldown literals | Uses per-deployment policy + `applyPolicyCooldown` |
| Anthropic `tokenUrl` | `ANTHROPIC_OAUTH_TOKEN_URL` env passed through subscription context |
| Responses hidden-only content | `hasHiddenOnlyResponsesInput` + handler guard |
| Denial `/nim/failures` | `failedRequestSummaryFromReceipt` + `persistDenialReceipt` store denials |
| `isQuarantined` | Aligned with `admit()` via `max(consecutive, circuitFailureCount)` |

## Verification commands

```bash
npm test
npm run tsc
```

## Git state

Audit fixes are on `main` (fast-forward from slice branches + integration commit `8ee3a73`). Push with:

```bash
git push origin main
```
