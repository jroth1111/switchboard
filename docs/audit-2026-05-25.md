# 20-slice parallel bug and logic audit

**Date:** 2026-05-25  
**Scope:** Full switchboard codebase (~103 TS files)  
**Method:** 20 disjoint file slices, 2 waves of 10 parallel subagents, bug + logic rubric, fix-all policy, integration gate (`tsc` + vitest)

## Summary

| Metric | Result |
|--------|--------|
| Slices audited | 20 / 20 |
| Tests | **866 / 866** pass (`npm test`) |
| Typecheck | **clean** (`npm run tsc`) |
| `main` head | `d3b390d` (residual audit follow-up merged) |

## Pass-2 integration (parent agent)

| Fix | File | Reason |
|-----|------|--------|
| Top-level `fetch` try/catch + `withCors` on 500 | `src/index.ts` | Slice 01: thrown errors lacked CORS headers |
| `typeMatches` / `resolveSchemaForValue` for `type: string[]` | `src/nim/repair/schema-aware.ts` | Slice 20 union types; `tsc` failed |
| OAuth lock test TTL 50ms / sleep 60ms | `tests/state/oauth-account-do.test.ts` | Flaky timing vs 20ms/30ms window |

## Residual follow-up (merged to `main`)

| Item | Resolution |
|------|------------|
| Planner `deployment-filter` | Wired into attempt loop + hedge admission via `buildFilterStateFromHealth` |
| Fixture worker | Shared `fixtureErrorResponse` for chat, responses, and messages |
| `classifyProviderFailure` | `isContextLengthError()` excludes OAuth/token auth bodies |
| `scripts/failures-probe.ts` | Removed (duplicate of `scripts/failures.ts`) |
| OAuth DO tests | `ENCRYPTION_KEY` in `wrangler.dev.jsonc`; crypto round-trip tests |
| Canary cooldown | Per-deployment policy + `applyPolicyCooldown` |
| Anthropic `tokenUrl` | `ANTHROPIC_OAUTH_TOKEN_URL` in subscription context |
| Responses hidden-only content | `hasHiddenOnlyResponsesInput` + handler guard |
| Denial `/nim/failures` | `finalizeFailedRequest` + shared `persistReceiptAsync` |
| `isQuarantined` | Aligned with `admit()` via `max(consecutive, circuitFailureCount)` |
| LiteLLM coupling | Removed `LITELLM_MASTER_KEY` health auth and LiteLLM config parity check; health uses `NIM_HEALTH_TOKEN` / `ADMIN_API_KEY` only |

## Verification

```bash
npm test
npm run tsc
```
